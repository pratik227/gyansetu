<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark-mode app theme. Inherits from a Material dark parent so the
         status bar / nav bar contrast correctly when the system is in dark
         mode. The Compose UI itself swaps palettes via isSystemInDarkTheme(). -->
    <style name="Theme.GyanSetu" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/status_bar</item>
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">@color/saffron_deep</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="31">@drawable/ic_launcher_foreground</item>
        <item name="android:windowSplashScreenIconBackgroundColor" tools:targetApi="31">@color/saffron_deep</item>
    </style>
</resources>
